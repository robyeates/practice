package algorithms.easy

import kotlin.test.assertEquals
import org.junit.Test as test

class Test15 {
    @test fun migratoryBirds() {
        assertEquals(4, migratoryBirds(arrayOf(1, 4, 4, 4, 5, 3)))
    }
}
